services:
  app:
    build: .
    environment:
      - DEBUG=true
      - BOT_SLUG=yout-bot-name
      - SDK_API_TOKEN=y0ur4p1tok3n
      - MAP_ZOOM_LEVEL=18
      - IMAGE_FORMAT=jpeg
      - IMAGE_QUALITY=0.8
    ports:
      - "8000:8000"
    volumes:
      - ./screenshots:/app/screenshots
