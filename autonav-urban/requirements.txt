# autonav-urban dependencies.
#
# Install AFTER the root repo venv is already set up (fastapi, pyppeteer,
# google-genai, etc.). Kept isolated because the root requirements.txt has
# pre-existing pins (pyppeteer 2.0 → websockets<11, google-genai → websockets>=13)
# that break full pip resolution when new deps are added.
#
# From repo root:
#   source venv39/bin/activate
#   pip install -r autonav-urban/requirements.txt
#
# On macOS Python 3.9 arm64 (Mini+ dev host): torch 2.3.x is the last line that
# still ships wheels for cp39. torch>=2.4 dropped Python 3.9.

torch>=2.2,<2.4
torchvision>=0.17,<0.19
hydra-core>=1.3
iopath>=0.1.10
scikit-learn>=1.3
matplotlib>=3.7
gdown>=5.0

# Pillow and PyYAML are already installed by the root venv; not repeated here.
